<version>3.3.0</version> ```

---

### ✅ Итог ревью

Ты молодец, что сразу бросился писать код. Ошибки, которые ты допустил (особенно с наследованием Сервиса), — это классика при переходе с других парадигм. Теперь ты знаешь, что Сервис — это "менеджер", который держит Репозиторий "за ручку", а не становится им.

**Твои следующие шаги (Correction Plan):**

1.  Исправь `pom.xml` (версию).
2.  Сделай `TaskRepository` интерфейсом.
3.  Добавь аннотации в `Task.java` и исправь поле даты.
4.  Перепиши `TaskService` (убери наследование, добавь поле `private final TaskRepository`).

**Челлендж для закрепления:**
В классе `TaskService` у тебя пока нет методов **`updateTask`** и **`deleteTask`**.
После того как исправишь ошибки, попробуй реализовать метод обновления:

```java
public Task updateTask(Long id, Task updatedTask) {
    // 1. Найти старую задачу по id.
    // 2. Если нет - выкинуть ошибку (или вернуть null).
    // 3. Если есть - обновить поля (title, description, completed) данными из updatedTask.
    // 4. Сохранить.
}