📅 План обучения: "Spring Sprint" (2 часа в день)
Ты просил домашнее задание. Мы создаем полноценный API. Цель: Через 5 дней у тебя будет готовое приложение, которое можно положить в портфолио.

День 1: Сборка и Первый запуск (Сегодня)
Задача:

Создай структуру папок в IDE (или обнови файлы кодом выше).

Создай главный класс TodoApplication.java (с public static void main и аннотацией @SpringBootApplication), если его нет.

Запусти проект. Убедись, что в консоли написано Started TodoApplication in ... seconds.

Если упало — прочитай ошибку, попытайся понять (или скинь мне).

Теория: Прочитать про Hibernate ddl-auto (как Spring создает таблицы при старте).

День 2: Контроллер и Postman
Задача:

Напиши TaskController.

Реализуй методы:

POST /tasks (Создать задачу)

GET /tasks (Получить все)

Установи Postman (или используй Insomnia).

Отправь JSON { "title": "Buy milk" } на сервер и убедись, что в ответ пришла задача с id: 1.

Теория: Изучи разницу между @RestController и @Controller.

День 3: Валидация и Обработка ошибок
Задача:

Попробуй отправить пустой title ({ "title": "" }). Получи 400 Bad Request.

Подключи наш GlobalExceptionHandler (из начала переписки), чтобы ответ был красивым JSON, а не стеком ошибок.

Добавь метод DELETE /tasks/{id} в контроллер и сервис.

Теория: Почитать про HTTP статус коды (200, 201, 400, 404, 500).

День 4: Бизнес-логика и Поиск
Задача:

В TaskRepository раскомментируй метод findByCompleted.

В Контроллер добавь метод GET /tasks/search?completed=true.

Реализуй логику: вернуть только выполненные задачи.

Теория: RequestParam vs PathVariable.

День 5: Финализация и H2 Console
Задача:

Включи консоль базы данных H2 (добавь настройки в application.properties).

Зайди в браузер, открой БД и посмотри SQL-таблицу глазами.

Напиши метод PATCH /tasks/{id}/complete, который просто ставит галочку "выполнено" (без отправки всего объекта).

Итог: У тебя полностью рабочий API.